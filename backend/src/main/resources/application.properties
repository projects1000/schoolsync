# Spring Boot Configuration
server.port=${PORT:8080}
spring.application.name=playschool-management

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://chlokeshreddy20:bMRwMCOYIwoCYp3e@cluster0.r0o4pin.mongodb.net/playschool?appName=Cluster0}

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173}
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*

# JWT Configuration
jwt.secret=${JWT_SECRET:littleStepsPlayschoolSecretKeyForJWTTokenGeneration2024!@#$%^&*()}
jwt.expiration=1800000

# Super Admin Configuration
app.superadmin.email=${SUPER_ADMIN_EMAIL:superadmin@aayratechx.com}
app.superadmin.password=${SUPER_ADMIN_PASSWORD:Aayratechx@superadmin}
app.superadmin.name=AayraTechX

# Logging Configuration
logging.level.com.littlesteps.playschool=DEBUG
logging.level.org.springframework.cache=TRACE
app.cache.ttl=10

# ===============================
# EMAIL SMTP CONFIGURATION
# ===============================
# For production, fill these with your actual SMTP credentials (e.g., Gmail App Password, AWS SES, SendGrid)
# If left blank, the system will print the verification URL to the console instead of sending an email.
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=your-email@gmail.com
# spring.mail.password=your-app-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true